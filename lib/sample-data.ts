import { Lead } from '../types/lead';

/**
 * Initial empty leads state.
 * Leads are strictly populated via live registry search (CMS NPPES, State Socrata) or verified CSV imports.
 */
export const INITIAL_LEADS: Lead[] = [];
