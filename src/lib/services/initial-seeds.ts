import type { Lead } from '../types/lead';

/**
 * Verified Lead Initial State
 * Mock data generation is strictly prohibited.
 * Leads must be populated exclusively from live regulatory registry endpoints.
 */
export const INITIAL_VERIFIED_LEADS: Lead[] = [];
